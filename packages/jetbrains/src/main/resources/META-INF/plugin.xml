<idea-plugin>
  <id>ai.opencode.gui</id>
  <name>opencode GUI</name>
  <vendor>opencode-gui</vendor>

  <description><![CDATA[
    A rich GUI for <a href="https://opencode.ai">opencode</a>: chat with image &amp;
    file attachments, file search, @-mentions, and slash commands — hosted in a
    JCEF tool window on top of the opencode server. Shares its UI with the
    VS Code extension.
  ]]></description>

  <depends>com.intellij.modules.platform</depends>

  <extensions defaultExtensionNs="com.intellij">
    <toolWindow
        id="opencode"
        anchor="right"
        icon="AllIcons.Toolwindows.ToolWindowMessages"
        factoryClass="ai.opencode.gui.OpencodeToolWindowFactory" />
  </extensions>
</idea-plugin>
